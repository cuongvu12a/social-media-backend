type BookCategory {
  id: String!
  name: String!
  parentId: String
  parentName: String
}

type Mutation {
  createBookCategory(data: CreateBookCategoryInput!): Boolean!  
}

input CreateBookCategoryInput{
  name: String!
}